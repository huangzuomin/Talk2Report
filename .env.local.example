# 快速修复：本地开发环境变量配置

# 复制这个文件为 .env.local，并填入你的实际配置

# n8n Webhook URLs (直连 n8n，跳过 BFF)
# 注意：Vite 要求环境变量必须以 VITE_ 开头才能在前端访问
VITE_N8N_INTERVIEW_URL=https://n8n.neican.ai/webhook/interview/next_step
VITE_N8N_GENERATE_URL=https://n8n.neican.ai/webhook/generate

# n8n 认证 Token
# 从 n8n Webhook 节点的 Header Auth 配置中获取
VITE_N8N_AUTH_TOKEN=NeicanSTT2025Secret

# 使用说明：
# 1. 复制此文件为 .env.local
# 2. 替换 your_actual_token_here 为你的实际 Token
# 3. 重启开发服务器: npm run dev
# 4. 访问 http://localhost:5173

# 注意事项：
# - .env.local 已在 .gitignore 中，不会被提交到 Git
# - 修改环境变量后必须重启开发服务器
# - 浏览器控制台会显示 "API Config" 日志，确认配置是否正确
